[build]
  command = "pnpm install --frozen-lockfile && pnpm --filter @workspace/api-spec run codegen && pnpm --filter @workspace/api-server run build:netlify && pnpm --filter @workspace/najammelha run build"
  publish = "artifacts/najammelha/dist/public"
  functions = "netlify/functions"

# vite.config.ts requires these (they come from Replit's workflow elsewhere).
[build.environment]
  PORT = "3000"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
